#!/usr/bin/env node
var debug = require('debug')('node_game');
var app = require('../app');

app.set('port', Number(process.env.PORT || 5000));

var server = app.listen(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});
